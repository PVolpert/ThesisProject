/**
 * Creates GUI for Modal with
 * - Tab 1: Form
 * - Tab 2: RequestList
 * Tabs are switchable
 */

import Button from '../../../UI/Button';

export default function AddAFriendDisplay() {
    return <></>;
}
