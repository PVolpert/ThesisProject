import Page from '../UI/Page';
import LoginDisplay from './LoginDisplay';

export default function LoginPageDisplay() {
    return (
        <Page className="md:mx-auto flex flex-col items-center justify-start">
            <LoginDisplay />
        </Page>
    );
}
